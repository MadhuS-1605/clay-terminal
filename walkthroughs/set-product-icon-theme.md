# Set Product Icon Theme

The product icon theme themes the editor UI's own glyphs — chevrons,
folder icons, and git status icons — separately from file icons. It's
also its own picker.

```button
{"title": "Set Product Icon Theme", "command": "workbench.action.selectProductIconTheme"}
```

Pick **Clay Terminal Icons** from the list that opens.
